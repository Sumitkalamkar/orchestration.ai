[project]
name = "report-gen"
version = "0.1.0"
description = "LangGraph orchestrator-worker report generator"
requires-python = ">=3.11"
dependencies = [
    "langchain-groq",
    "langgraph",
    "python-dotenv",
    "fastapi",
    "uvicorn",
]
